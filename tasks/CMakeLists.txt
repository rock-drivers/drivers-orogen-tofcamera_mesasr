# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt
include(tofcamera_mesa_swissrangerTaskLib)
ADD_LIBRARY(${TOFCAMERA_MESA_SWISSRANGER_TASKLIB_NAME} SHARED 
    ${TOFCAMERA_MESA_SWISSRANGER_TASKLIB_SOURCES})

add_dependencies(${TOFCAMERA_MESA_SWISSRANGER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${TOFCAMERA_MESA_SWISSRANGER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TOFCAMERA_MESA_SWISSRANGER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TOFCAMERA_MESA_SWISSRANGER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TOFCAMERA_MESA_SWISSRANGER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TOFCAMERA_MESA_SWISSRANGER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TOFCAMERA_MESA_SWISSRANGER_TASKLIB_HEADERS}
    DESTINATION include/orocos/tofcamera_mesa_swissranger)

