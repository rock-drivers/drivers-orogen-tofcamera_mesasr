PROJECT(tofcamera_mesa_swissranger)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/.orogen/config")
INCLUDE(tofcamera_mesa_swissrangerBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

